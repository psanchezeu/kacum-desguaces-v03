// API URL configuration
// En Vite, las variables de entorno se acceden mediante import.meta.env
// La URL de la API se inyecta durante el proceso de build a través de variables de entorno.
// Asegúrate de que VITE_API_URL esté configurada en tu entorno de despliegue (ej. Coolify).
export const API_URL = import.meta.env.VITE_API_URL;
